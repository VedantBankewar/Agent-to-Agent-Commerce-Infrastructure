# AgentTrade dependencies
# Python 3.11+

# AI / Agent Layer
langchain>=0.3.0
langchain-anthropic>=0.3.0
langchain-openai>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.0

# Blockchain Layer
algokit>=2.0.0
pyteal>=0.26.1

# Agent-native payments — x402 on Algorand (client + server + facilitator)
# NOTE: confirm the installed import root on first run (x402 vs x402_avm); see x402/README.
x402-avm[all]

# Web / API Layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0

# Data Layer
redis>=5.0.0
sqlalchemy>=2.0.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
